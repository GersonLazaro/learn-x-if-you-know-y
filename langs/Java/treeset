TreeSet<Integer> t = new TreeSet<Integer>(); 
t.add(32); // Insert
t.contains(32); // Check if an item exists
t.isEmpty(); // Verify if the TreeSet is empty
t.remove(25); // Delete value
t.clear(); // Delete all
t.size(); // Get size