ArrayList<Integer> a = newArrayList<>();
// Insert
a.add(32);
// Get value in an index
a.get(1);
// Modify the given element (0) with the given value (25)
a.set(0,25);
// Delete value at a given index
a.remove(1);
// Delete all
a.clear();
// Get size
a.size();