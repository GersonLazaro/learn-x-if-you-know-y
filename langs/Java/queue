Queue<Integer> q = new LinkedList<Integer>();
q.add(15); //Insert
q.poll(); //Get and remove the front element
q.peek(); //Get the front element without remove it
q.isEmpty(); //Verify if the queue is empty
